= simple_form_for post do |f|
  - if post.errors.any?
    %ul
      - post.errors.full_messages.each do |message|
        %li= message
  = f.input :content, placeholder: '文章を入力してください'
  = f.button :submit, class: 'btn btn-primary'
