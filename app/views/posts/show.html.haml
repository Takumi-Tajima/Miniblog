.card.mb-4
  .card-body
    .card-text
      = @post.content

= link_to '編集', edit_post_path(@post), class: 'btn btn-success'
= button_to '削除', post_path(@post), method: :delete, class: 'btn btn-danger mt-4'
