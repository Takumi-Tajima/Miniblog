%h1.h3 自分ページ

.row.justify-content-center
  .col-md-8
    .card
      .card-body
        .card-title
          %h1.h3= @user.name
        %p= @user.email
        %p= @user.biography
        %p= @user.blog_url
