= simple_form_for @user do |f|
  - if @user.errors.any?
    %ul
      - @user.errors.full_messages.each do |message|
        %li= message
  = f.input :biography, placeholder: '自己紹介を入力してください'
  = f.input :blog_url, placeholder: 'ブログのURLを入力してください'
  = f.button :submit, class: 'btn btn-primary'
